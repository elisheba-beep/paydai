// ignore_for_file: file_names

import 'package:flutter/material.dart';

class PayDaiLogo extends StatelessWidget {
  const PayDaiLogo({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.rectangle_outlined,
      color: Colors.white,
    );
  }
}
